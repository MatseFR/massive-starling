This sample requires the [valeditor](https://github.com/MatseFR/valeditor) lib

This is a work in progress : you can already use it to save and load configurations but you can't import textures for now (if you want to do that you can extend or change the code)